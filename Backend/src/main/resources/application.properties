spring.application.name=Kid_coding

# ===============================
# SERVER
# ===============================
# Render assigns a port dynamically; this line ensures your app uses it.
server.port=${PORT:8080}
server.address=0.0.0.0

# ===============================
# DATABASE (PostgreSQL)
# ===============================
# Using the INTERNAL host for faster, more stable connections within Render
spring.datasource.url=jdbc:postgresql://dpg-d6c68ornv86c73cs01k0-a:5432/kidcodingdb
spring.datasource.username=kidcodingdb_user
spring.datasource.password=BRYU2NXwbUW7L2NI2qmxU3Cz6n87Nqcu
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# JPA / HIBERNATE
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.main.allow-bean-definition-overriding=true

# ===============================
# MAIL SETTINGS
# ===============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# It is better to use the Env Variables we saw in your screenshot:
spring.mail.username=${SPRING_MAIL_USERNAME}
spring.mail.password=${SPRING_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ===============================
# FRONTEND URL
# ===============================
FRONTEND_URL=https://blockjava.vercel.app
